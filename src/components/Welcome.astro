---
import { SignedIn } from "@clerk/astro/components";
const user = await Astro.locals.currentUser();
const username = user?.fullName;
---

<SignedIn>
    Welcome {username}! Please proceed to the <a href="/dashboard">dashboard</a
    >.
</SignedIn>
